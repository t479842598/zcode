# Zcode_满血_青棠 v3.14.7 — 官方奖励页登录上下文修复

- 对齐官方奖励页注入逻辑：读取当前用户、当前 OAuth provider、provider access token 和 ZCode JWT。
- 只有当前用户存在且 access token/JWT 至少一个存在时才标记奖励页为已登录。
- 保留官方 rewards 页面自身的活动渲染与领取动作，不在本地伪造额度。
