# Zcode_满血_青棠 v3.14.5 — 登录状态与当前版本标识修复

## 本次更新

- 修复官方活动页把已登录账号误判为需要再次登录的问题：当前 provider access token 或 ZCode JWT 任一有效即可注入登录上下文。
- active provider 缺失时，从现有 OAuth token 自动推断 Z.ai/BigModel provider。
- 新增对话欢迎态的 GitHub 地址后标注“当前”，并显示客户端当前版本徽标。

## 验证

- rewards 路由/凭据测试、草稿归属栏测试通过。
- `pnpm typecheck` 和 `oxfmt --check` 通过。
